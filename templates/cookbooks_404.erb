<div id="content" class="scm fof">
  <h1>Oops,</h1>
  <% if settings.cookbooks_adapter.libraries.cookbook?(@cookbookname) %>
    <h2 class="nomargin">We Couldn't Find That Page</h2>
    <p>Sorry, but we couldn't find the page <strong><%= h request.path %></strong> in the <strong><%= h @cookbookname %></strong> cookbook. Are you sure that class, method, or file exists?</p>
  <% else %>
    <h2 class="nomargin">We Couldn't Find That Cookbook</h2>
    <p>We can't seem to find any versions for the cookbook <strong><%= h @cookbookname %></strong>. Are you sure you typed it correctly?</p>
  <% end %>

  <p>You can also see a list of available Cookbooks <a href="/cookbooks">here</a>.</p>
</div>
